<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rent.mapper.UsersMapper">

    <resultMap id="BaseResultMap" type="com.rent.common.entity.Users">
            <id property="id" column="id" />
            <result property="username" column="username" />
            <result property="password" column="password" />
            <result property="phone" column="phone" />
            <result property="nickname" column="nickname" />
            <result property="avatar" column="avatar" />
            <result property="role" column="role" />
            <result property="balance" column="balance" />
            <result property="status" column="status" />
            <result property="createdAt" column="created_at" />
            <result property="updatedAt" column="updated_at" />
    </resultMap>

    <sql id="Base_Column_List">
        id,username,password,phone,nickname,avatar,
        role,balance,status,created_at,updated_at
    </sql>
    <select id="loginByAdmin" resultMap="BaseResultMap">
        select * from users where username=#{username} and role = 20 and status = 1
    </select>
    <select id="queryUserNum" resultType="java.lang.Integer">
        select count(*) from users where role = 10 and status = 1
    </select>
    <select id="loginByUserPhone" resultMap="BaseResultMap">
        select * from users where phone=#{phone} and role = 10 and status = 1
    </select>
    <select id="getUserById" resultMap="BaseResultMap">
        select * from users where id=#{id} and status = 1
    </select>
</mapper>
